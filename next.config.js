/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["fakestoreapi.com", "i.ibb.co", "storybook7.blob.core.windows.net"],
      },
}

module.exports = nextConfig
